import React from 'react';

export const Header = () => (
    <header>
        <div>
            <span className="logo">House Staff</span>
        </div>

        <div className="presentation"></div>
    </header>
);
